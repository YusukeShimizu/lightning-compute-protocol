# Example env file for running lcpd-grpcd (used by ./scripts/lcpd-grpcd).
#
# Copy to ./lcpd.env and edit:
#   cp lcpd.env.sample lcpd.env
#   chmod 600 lcpd.env
#
# IMPORTANT:
# - Never commit real secrets (API keys, macaroons).
# - Keep permissions tight (600) since this file may contain credentials.

# Logging
LCPD_LOG_LEVEL=info

# Provider backend:
# - disabled: requester-only (no execution)
# - openai: provider execution via OpenAI-compatible API
# - deterministic: dev/testing backend
LCPD_BACKEND=disabled

# Provider config (YAML). If unset, lcpd-grpcd uses ./config.yaml if present.
# The repo's default go-lcpd/config.yaml keeps the Provider disabled.
# LCPD_PROVIDER_CONFIG_PATH="$PWD/config.yaml"

# lnd integration (peer messaging / invoice payments)
# LCPD_LND_RPC_ADDR=localhost:10009
# LCPD_LND_TLS_CERT_PATH="$HOME/.lnd/tls.cert"
# LCPD_LND_ADMIN_MACAROON_PATH="$HOME/.lnd/data/chain/bitcoin/mainnet/admin.macaroon"

# OpenAI backend settings (only needed if LCPD_BACKEND=openai)
# LCPD_OPENAI_API_KEY="..."
# LCPD_OPENAI_BASE_URL="https://api.openai.com"
